# Operator-local configuration and secrets (never commit)
.env
.env.*
!.env.example
scripts/staging/bootstrap.env
runtime-config.js
**/runtime-config.js

# Dependencies and local tooling
node_modules/
test-results/
playwright-report/
.supabase/
supabase/.branches/
supabase/.temp/
.DS_Store

# Editor / agent local state
.idea/
.vscode/
*.swp

.vercel

scripts/platform/.onboarding-invite-links.md

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
